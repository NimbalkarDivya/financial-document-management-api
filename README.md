# financial-document-management-api
A FastAPI-based backend for managing financial documents with authentication, role-based access, and semantic search using embeddings.

#how to run the project:
#install dependencies:  pip install -r requirements.txt
#then run: venv\Scripts\activate  
#run: uvicorn app.main:app --reload  or python run.py
